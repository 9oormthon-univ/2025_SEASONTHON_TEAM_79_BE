spring:
#  datasource:
#    url: ${MYSQL_URL}
#    username: ${MYSQL_USERNAME}
#    password: ${MYSQL_PASSWORD}
#    driver-class-name: com.mysql.cj.jdbc.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
#        format_sql: true
#        show_sql: true
  datasource:
    url: jdbc:mysql://seasonthon-db.c50wyc4mamqw.ap-southeast-2.rds.amazonaws.com:3306/seasonthon-db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: admin
    password: seasonthon79
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        show_sql: true

  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}

  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          #          ssl:
          #            enable: true
          starttls:
            enable: true
            required: true
      default-encoding: UTF-8

jwt:
  secret: ${JWT_SECRET_KEY}
  access-token:
    header: Authorization
    expiration-time: 900 #60 * 15 (15분)
  refresh-token:
    header: Authorization_refresh
    expiration-time: 604800 #60 * 60 * 24 * 7 (7일)
